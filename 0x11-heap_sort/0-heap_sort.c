#include <stdlib.h>
#include <stdio.h>
#include "sort.h"

/**
 * heap_sort - implement the sift-down heap sort algorithm
 *
 * @array: ptr to int array
 * @size: size of the array
 * Return: void
 */
void heap_sort(int *array, size_t size)
{
	if (!array || !size)
		exit(0);
}
