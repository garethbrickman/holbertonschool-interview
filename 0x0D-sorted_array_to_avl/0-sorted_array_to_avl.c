#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "binary_trees.h"


/**
 * sorted_array_to_avl - builds an AVL tree from array
 *
 * @array: ptr to first element of array
 * @size: num of elements in array
 *
 * Return: ptr to root node of AVL tree, or NULL
 */
avl_t *sorted_array_to_avl(int *array, size_t size)
{
	if (!array || !size)
		return(NULL);
	return(0);
}
